/*
 * Public interface to the application layer.
 *
 * app_init() performs application-specific initialization.
 * app_update() contains the main application logic and is called repeatedly.
 */

#pragma once

void app_init(void);
void app_update(void);
